# Chat A / Chat B prompts — 2026-09-09, after the completeness gate

## DECISION NEEDED FIRST — the gate blocked batch 2

The 3-of-3 gate retired **31 objects**, and it did not only hit the empty ones.

| what it retired | count | example |
|---|---|---|
| answered **0 of 3** | 20 | every Industrials object — the intended target |
| answered **1 or 2 of 3** | 11 | `regional_banking` UNKNOWN/MODERATE/ELEVATED |

**Batch 2 is now blocked: 13 of its 42 companies** resolve to a retired object —
11 to `regional_banking`, 2 to `mortgage_reits`. Both were previously-shipped, working
Financials objects that answered 2 of 3 and abstained on `structural_growth`.

**The tension is real and it is the project's own rule.** `CODEX_STANDARD_RULES` says a
reasoned UNKNOWN is a result, not a gap — E53 proved it, where `market_share_direction`
was correctly 0 of 59 because a regulated utility has no contestable share. A strict 3-of-3
gate cannot tell **"abstained wholesale"** (Industrials, 20 objects at 0 of 3) from
**"abstained on one field for a good reason"** (`regional_banking` at 2 of 3), and it
deletes both.

### Recommended: keep 0-of-3 deleted, restore the 11 partials

Delete an object that **concludes nothing** — that was the actual E61 problem, and it is
20 of the 31. Restore the 11 that answered something, and require a **written reason per
UNKNOWN field** on any future object below 3 of 3. That keeps the user's bar (no empty
objects), unblocks batch 2, and stops the gate punishing the honest refusal the standard
rules explicitly protect.

Restoring is one call — the rows are SUPERSEDED, not deleted, and every claim is intact.

**Until this is decided, batch 2 must not be handed over.** `build_roster` will refuse it,
which is the guard working.

---

# CHAT B — blocked on the decision above

Do **not** hand batch 2 to Codex yet. Request `b1d3275420b64cdb92f3` is frozen against a
roster that no longer resolves.

**If the recommendation is taken**, the 11 partials come back, `regional_banking` and
`mortgage_reits` are active again, and the existing request is valid as-is — hand over
`docs/CODEX_E59_FINANCIALS_PHASEB_B2.md` unchanged.

**If the strict 3-of-3 bar stands**, batch 2 has to be rebuilt without the 13 blocked
companies, and `regional_banking` — 45 remaining companies, the largest Financials
industry — needs re-researching before any of them can be batched.

## Local work either way

**B6, the recount of overstated totals.** Answered-field counts, search counts, endpoint
coverage. Cite `CORPUS_SNAPSHOT_2026-09-09.json`; do not quote a bare rate.

## Do not

**E58 arm B needs all four Financials batches.** It is further away now, not closer.

---

# CHAT A — Information Technology Phase A, and it is not blocked

IT is untouched by the gate: **5 of 5 IT objects passed 3 of 3.** This is the cleanest
next Phase A and it now has a second job.

```
information_technology   190 companies   12 objects needed
  application_software, communications_equipment, electronic_components,
  electronic_equipment_instruments, electronic_manufacturing_services,
  internet_services_infrastructure, ...
```

## The prompt must carry four rules, three earned and one new

- **Every structural field is answered, or the object is retired automatically.** This is
  now enforced in code, not asked for politely — `batch_health.enforce()` retires anything
  under 3 of 3. Say so in the prompt. E61 shipped 20 objects that concluded nothing and it
  is the reason.
- **An UNKNOWN needs a written reason naming what was looked for.** A reasoned refusal is
  still a result — E53's utilities were correctly 0 of 59 — but it must be argued, not
  left blank. This is the rule that separates the two cases the gate cannot.
- **Depth follows the evidence.** A uniform claim count is a quota, and a quota has
  produced a defect twice: E54's uniform 5, E57's one-claim-per-field.
- **Context is not support**, and **source diversity is part of the deliverable.** E57 lost
  30 of 66 claims to context-only evidence that all verified literally. E61 shipped 140 of
  145 claims on one independence domain.

## Then

**Industrials needs re-researching** — 27 objects, 263 companies, currently 0 active
objects. It joins Health Care (E76) as a sector to rebuild. Both are now gate-enforced, so
a repeat of E61 retires itself automatically rather than reaching the workbook.

---

## Both chats

- Verify against the store, not the report. Five reported numbers have been corrected this
  way now.
- Advisory / SHADOW. `promoted` is false and no code path sets it true.
- Every count carries its corpus state.
- Rule 4 — an uncommitted change in a shared path is unowned until someone claims it.
- Closing a real defect and closing a non-defect look identical in a changelog. Say which.
